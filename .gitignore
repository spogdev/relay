.gradle/
build/
out/
run/
# Release jars collected from both branches' builds: build/ is per-branch and cleaned, so the
# two clients cannot coexist there.
dist/
.idea/
*.iml
.DS_Store
# Local AI-assistant config. The architecture notes CLAUDE.md used to carry live in
# docs/ARCHITECTURE.md instead, so they are readable without the tooling.
.claude/
CLAUDE.md
